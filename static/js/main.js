const update_buttons = Array.prototype.slice.call(document.getElementsByClassName("update-button"));
update_buttons.forEach((e) => {
    e.addEventListener("click", ()=>{
        alert("Not Implemented"); // Da implementare la funzionalità di "Tienimi Aggiornato"
    })
})